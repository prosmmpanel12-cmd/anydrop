<?xml version="1.0" encoding="utf-8"?>
<!-- Companion thumb for switch_track_pill.xml — a plain white circle
     sized just under the 30dp track height (24dp, leaving a ~3dp ring of
     track color around it on every side) so it reads as the big
     iOS-style knob from the app-owner's reference image, not the small
     Material default thumb. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/white" />
    <size
        android:width="24dp"
        android:height="24dp" />
</shape>
