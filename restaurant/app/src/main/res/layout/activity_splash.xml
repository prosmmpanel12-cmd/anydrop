<?xml version="1.0" encoding="utf-8"?>
<!-- Pre-login ink pass (2026-08-16, docs/restorent/19 §10 item 7): background
     was flat @color/anydrop_primary (orange); switched to @color/anydrop_ink
     so the launcher's first frame matches the app's main dark chrome
     (activity_main.xml's top bar/bottom nav) rather than the pre-refresh
     orange splash. Tagline recolored to text_on_ink_muted (was
     anydrop_primary_container, a light peach tuned for an orange
     background) to keep it clearly secondary against dark ink. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/anydrop_ink"
    android:gravity="center">

    <ImageView
        android:id="@+id/splashLogo"
        android:layout_width="96dp"
        android:layout_height="96dp"
        android:src="@mipmap/ic_launcher"
        android:contentDescription="@string/app_name"
        android:visibility="invisible" />

    <TextView
        android:id="@+id/splashTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="18dp"
        android:text="@string/app_name"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/text_on_ink"
        android:visibility="invisible" />

    <TextView
        android:id="@+id/splashTagline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="@string/splash_tagline"
        android:textSize="14sp"
        android:textColor="@color/text_on_ink_muted"
        android:visibility="invisible" />

</LinearLayout>
