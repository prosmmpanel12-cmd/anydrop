<!-- Reverted 2026-08-16 (§8.1.1) back to the original orange gradient —
     app owner preferred orange+white over the ink pass after real-device
     testing. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <gradient
        android:angle="315"
        android:startColor="@color/anydrop_primary"
        android:endColor="@color/anydrop_primary_dark" />
    <corners
        android:bottomLeftRadius="32dp"
        android:bottomRightRadius="32dp" />
</shape>
