<?xml version="1.0" encoding="utf-8"?>
<!-- Shared top bar OPEN/CLOSED pill (docs/restorent/19, §3) — red
     "closed / paused, not accepting orders" state. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/error_bg" />
    <stroke android:width="1dp" android:color="@color/nonveg_red" />
    <corners android:radius="20dp" />
</shape>
