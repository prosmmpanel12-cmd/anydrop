<?xml version="1.0" encoding="utf-8"?>
<!-- Unfilled track for a single Stories-style progress segment
     (DishPhotoCarouselView's carouselProgressSegments) — translucent white
     so it reads on any food-photo background, same palette as the old
     dot_carousel_unselected.xml it replaces. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#4DFFFFFF" />
    <corners android:radius="2dp" />
</shape>
