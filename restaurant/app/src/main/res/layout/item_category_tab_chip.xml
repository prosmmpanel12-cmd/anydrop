<?xml version="1.0" encoding="utf-8"?>
<!-- §5 — "Category tabs as a horizontal scroll strip... once a restaurant
     has 5+ categories." One chip per active category, plus an "All" chip
     (see CategoryTabAdapter). Selected state swaps background + text color,
     same selected/unselected pattern as the Customer app's filter chips. -->
<TextView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/tabChipText"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginEnd="8dp"
    android:paddingHorizontal="16dp"
    android:paddingVertical="8dp"
    android:textSize="13sp"
    android:textStyle="bold"
    android:singleLine="true"
    android:background="@drawable/bg_chip_unselected"
    android:textColor="@color/text_secondary" />
