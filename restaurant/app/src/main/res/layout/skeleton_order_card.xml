<?xml version="1.0" encoding="utf-8"?>
<!-- Mirrors item_order_card.xml's proportions exactly (docs/restorent/19, §9.2):
     a gray bar for the order #, a shorter gray bar for item count/total,
     and a rounded gray blob standing in for the status stepper.
     Meant to be repeated (e.g. 3x) inside a ShimmerFrameLayout. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_card_rounded"
    android:layout_marginHorizontal="16dp"
    android:layout_marginTop="10dp"
    android:padding="14dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- order # -->
        <View
            android:layout_width="0dp"
            android:layout_height="16dp"
            android:layout_weight="1"
            android:background="@drawable/bg_skeleton_bar" />

        <View
            android:layout_width="60dp"
            android:layout_height="22dp"
            android:layout_marginStart="10dp"
            android:background="@drawable/bg_skeleton_blob" />
    </LinearLayout>

    <!-- item count / total summary line -->
    <View
        android:layout_width="180dp"
        android:layout_height="13dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/bg_skeleton_bar" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="12dp"
        android:gravity="center_vertical">

        <!-- status stepper -->
        <View
            android:layout_width="0dp"
            android:layout_height="18dp"
            android:layout_weight="1"
            android:background="@drawable/bg_skeleton_blob" />

        <View
            android:layout_width="48dp"
            android:layout_height="13dp"
            android:layout_marginStart="10dp"
            android:background="@drawable/bg_skeleton_bar" />
    </LinearLayout>

</LinearLayout>
