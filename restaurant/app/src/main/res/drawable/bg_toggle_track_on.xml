<?xml version="1.0" encoding="utf-8"?>
<!-- Filled-pill toggle track, ON state (2026-08-19 toggle-restyle ask —
     match reference mockup's flat green/red slide-switch look: a solid
     rounded-rect track filling the whole switch height, not the default
     thin Material line-track. Used as android:track on every
     SwitchMaterial in this app via style_toggle_switch, paired with
     bg_toggle_track_off.xml (unchecked) through toggle_track_selector.xml. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/toggle_track_on" />
    <corners android:radius="14dp" />
</shape>
